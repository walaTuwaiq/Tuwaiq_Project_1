Bonus

if u have data in local storage give it
ifn't give me this obj


---------------


fadein
fadeout

there are function in JQuery, i can use these with scroll:
"""
$(document).scroll(function() {
    const flowerFullImage = $(".flowerFullImage")
    var y = $(this).scrollTop();
    console.log(this);
    console.log(y);
    if (y > 80) {
        flowerFullImage.fadeIn();
    } 
  });
"""

---------------------

slideDown()
it's so funny